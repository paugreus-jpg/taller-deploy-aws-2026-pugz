name: Docker Comparison CI

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build application (JAR)
        run: mvn clean package -DskipTests

      - name: Run tests
        run: mvn test

      - name: Build multistage Docker image
        run: docker build -f Dockerfile -t app-multi:latest .

      - name: Show Docker image sizes
        run: docker images | grep app-

      - name: Show summary
        run: |
          echo "=== IMAGE COMPARISON ==="
          docker images | grep app- || true